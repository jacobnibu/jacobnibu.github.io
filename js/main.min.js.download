$(document).ready(function() {
    var o = !1;
    /Android|iPhone|iPad|iPod|BlackBerry|Windows Phone/i.test(navigator.userAgent || navigator.vendor || window.opera) && ($("body").addClass("is-touch"), o = !0), $('a[href^="#"]').click(function(o) {
        o.preventDefault();
        var t = $(this).attr("href");
        $("html, body").animate({
            scrollTop: $(t).offset().top
        }, 800)
    }), $(".toggle").on("click", function() {
        var o = $(this).next(".list-toggle"),
            t = $(this).parent().find(".lazy-load-c img"),
            a = t.attr("data-src");
        o.slideToggle(300), $(this).toggleClass("is-toggled"), t.attr("src", a), t.parent().removeClass("lazy-load-c")
    }), $(".area-image-hover").on("mousemove", function(o) {
        var t = $(this).offset(),
            a = o.pageX - t.left,
            s = o.pageY - t.top;
        $(this).find("img").css({
            left: a + 20,
            top: s + 10
        })
    })
}), window.onload = window.onpageshow = function() {}, $(window).on("scroll load", function() {
    var o = $(window).scrollTop(),
        t = $(".anchor-content").offset().top,
        a = $(window).height() / 2,
        s = $(".link-site"),
        i = $(".nav ul li");
    $(".lazy-load-s").each(function() {
        var t = $(window).height(),
            a = $(this).offset().top - t,
            s = $(this).find("img").attr("data-src");
        o > a && $(this).hasClass("lazy-load-s") && ($(this).find("img").attr("src", s), $(this).removeClass("lazy-load-s"))
    }), $(".anchor").each(function(s) {
        var e = $(this).offset().top - a;
        o >= e && (i.removeClass("is-active"), i.eq(s).addClass("is-active")), o < t - a && i.eq(0).removeClass("is-active")
    }), o >= t - a ? s.addClass("is-show") : s.removeClass("is-show")
});
